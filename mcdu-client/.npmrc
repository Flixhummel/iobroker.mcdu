# Force node-hid to use libusb backend on Linux (hidraw backend fails for
# WinWing MCDU-32-CAPTAIN display writes on Raspberry Pi).
# On macOS, IOHIDManager is used regardless of this setting.
driver=libusb
